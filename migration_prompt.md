# 防災機能移植用プロンプト

以下の要件を満たす防災機能を、現在のシステムの構成・デザイン・認証方式に合わせて実装してください。
既存システムの関係ない機能は変更せず、今回指定する防災機能だけを追加・更新してください。

## 対象機能
次の機能だけを実装対象にしてください。

- ホーム画面
- 現在地表示
- 青森県青森市の気象警報・注意報表示
- お知らせ表示
- 掲示板表示
- 書き込み・通報機能
- 避難所の登録・表示

次の機能は実装対象外です。
- マイナンバー登録
- ログイン画面、ログイン機能
- メール通知登録
## ホーム画面

ホーム画面に次の項目を配置してください。
1. 現在地
2. お知らせ
3. 気象警報・注意報
4. 掲示板
5. 書き込み・通報への導線

上段に「現在地」と「お知らせ」を横並びで配置し、その下に気象警報・注意報、掲示板を配置してください。
掲示板の近く、できれば掲示板の直下に「書き込み・通報」ボタンを配置してください。

ホーム画面のタイトルは「防災アプリ」とし、既存システムの画面デザインに合わせてください。
## 現在地と地図

- 現在地は青森駅とする。
- 青森駅の座標は緯度 `40.8296`、経度 `140.7345` を使用する。
- 地図の中心を青森駅に設定する。
- 地図は周辺が確認できるよう、ズームレベル13程度で表示する。
- 現在地は青色の丸で表示する。
- 現在地をクリックまたは選択した場合、「青森駅（現在地）」と表示する。
- 登録されている避難所は、現在地周辺に赤色のピンで表示する。
- 避難所の赤いピンをクリックすると、避難所名を表示する。
- 地図ライブラリを利用できる場合は、既存システムで使用しているライブラリを優先する。
- 地図が利用できない場合でも、地図以外のホーム画面は利用できるようにする。

避難所データには最低限、次の情報を保持してください。
```json
{
  "id": 1,
  "name": "避難所名"
}
```

地図表示に緯度・経度が必要な場合は、既存データ形式を壊さず、地図表示用の座標を別途用意してください。座標がない避難所の位置を推測して表示しないでください。
## 避難所登録

避難所登録画面で避難所名を入力して登録できるようにしてください。

- 避難所名は必須入力にする。
- IDは既存IDの最大値に1を加えて自動採番する。
- 登録内容を既存システムのデータ保存方式で保存する。
- 登録成功時に成功メッセージを表示する。
- 保存に失敗した場合はエラーメッセージを表示する。
- 登録した避難所を一覧・地図に反映する。

## 気象警報・注意報

気象庁の警報・注意報データを利用し、青森県青森市の情報を表示してください。
- 都道府県コードは `020000`（青森県）。
- 市区町村コードは `0220100`（青森市）。
- 気象庁の公開データ/APIを使用する。
- 発表中または継続中の警報・注意報だけを表示する。
- 解除済みの情報は表示しない。
- 警報・注意報の名前と状態を表示する。
- 情報取得に失敗した場合は、取得失敗が分かるメッセージを表示する。
- 警報・注意報がない場合は、必ず次の文言を表示する。

```text
現在発令している警報・注意報はございません
```

確認した日時を日本時間で表示してください。表示形式は次の形式にしてください。

```text
2026年9月9日 18時30分 現在
```

実際の現在日時を使用し、警報がある場合もない場合も確認日時を表示してください。
## お知らせ

住民向けのお知らせをデータとして管理し、ホーム画面と掲示板に表示してください。

お知らせには最低限、次の情報を持たせてください。
```json
{
  "id": 1,
  "target": "住民",
  "content": "お知らせ本文",
  "shelter": "避難先名",
  "priority": 1,
  "status": "発表",
  "created_at": "2026年9月9日 18:30",
  "updated_at": "2026年9月9日 18:30"
}
```

表示ルール:

- `target` が「住民」のものだけを表示する。
- 重要度 `priority` の高い順に表示する。
- 同じ重要度の場合は更新日時の新しい順に表示する。
- 重要度3以上のお知らせは、赤文字で表示する。
- 重要度3以上のお知らせには「重要」と表示する。
- お知らせの各項目に更新日時を表示する。
- 更新日時がない場合は登録日時を表示する。
- ホーム画面では最大4件を初期表示する。
- 重要度3以上のお知らせは、常に初期表示の対象に含める。
- 「すべて表示」を押すと、登録されているお知らせをすべて表示する。
- 全件表示中はボタンを「一部表示に戻す」に変更する。
- 「一部表示に戻す」を押すと、最大4件表示に戻す。
- お知らせがない場合は、お知らせがないことを表示する。

重要なお知らせを最大4件の初期表示に必ず含めるため、重要度3以上を優先して選び、残りを通常のお知らせで補充してください。その後、表示順は重要度順と更新日時順にしてください。
## 掲示板

掲示板には住民向けのお知らせを重要度順で表示してください。

- 各項目に日時を表示する。
- 重要度3以上は赤文字で表示する。
- 重要度3以上には「重要」または「【重要】」を付ける。
- 初期状態では一部の項目を表示する。
- 「すべて表示」で全件を表示する。
- 「一部表示に戻す」で初期表示に戻す。
- ホーム画面の掲示板付近に書き込み・通報ボタンを配置する。
## 書き込み・通報

通報画面では自由記述だけでなく、次の定型フォーマットで入力できるようにしてください。

- 通報種別
- 発生場所
- 発生日時
- 詳細

通報種別は次の選択肢を用意してください。

- 浸水・洪水
- 道路・建物の被害
- 土砂災害
- 避難所・避難経路
- けが・救助
- その他

入力ルール:

- 通報種別は必須。
- 発生場所は必須。
- 発生日時は必須。
- 詳細は任意入力。
- 入力エラー時は、何が不足しているか分かるメッセージを表示する。
- 送信した通報は既存システムの保存方式で保存する。
- 保存するデータには通報ID、通報種別、発生場所、発生日時、詳細、受付日時、状態を含める。
- 初期状態の受付状態は「受付」とする。
- 通報画面に「⌂ ホームに戻る」というホームへのリンクを配置する。

保存データの例:

```json
{
  "id": 1,
  "report_type": "浸水・洪水",
  "location": "青森駅東口",
  "occurred_at": "2026-09-10T10:30",
  "content": "道路が冠水しています",
  "created_at": "2026年9月10日 10:35",
  "status": "受付"
}
```

## 実装上の注意

- 既存のフレームワーク、ルーティング、テンプレート、データ保存方式を優先して使用する。
- 既存の認証方式がある場合はそれを尊重する。ただし、今回の移植で新しいログイン画面やログイン機能を追加しない。
- 既存機能を壊さない。
- 入力値はサーバー側でも検証する。
- HTMLへ表示する値は、使用フレームワークの適切なエスケープ機能を使う。
- 画面表示とデータ保存の両方を実装する。
- レスポンシブ表示に対応する。
- 日時は保存形式と表示形式を区別し、表示は日本時間に統一する。
- 追加後に、ホーム画面、避難所登録、気象情報、掲示板、通報画面を実際に確認する。
- メール通知登録、マイナンバー登録、ログイン画面は追加しない。

## 完了条件

次の動作確認を実施してください。

- ホーム画面が正常に表示される。
- 上段に現在地とお知らせが横並びで表示される。
- 青森駅が現在地として地図の中心に表示される。
- 現在地が青丸で表示される。
- 現在地を選択すると「青森駅（現在地）」と表示される。
- 避難所が赤いピンで表示される。
- 赤いピンを選択すると避難所名が表示される。
- 青森市の警報・注意報が表示される。
- 警報・注意報がない場合、指定文言が表示される。
- 警報情報の確認日時が日本時間で表示される。
- お知らせが重要度順に表示される。
- 重要なお知らせが赤文字と重要ラベルで表示される。
- お知らせの日時が表示される。
- お知らせの「すべて表示」と「一部表示に戻す」が動作する。
- 掲示板の「すべて表示」と「一部表示に戻す」が動作する。
- 避難所を登録できる。
- 登録した避難所が一覧と地図に反映される。
- 通報種別・場所・発生日時・詳細を入力して通報できる。
- 詳細を空欄にしても通報できる。
- 必須項目が空の場合、具体的なエラーメッセージが表示される。
- 通報画面からホームへ戻れる。
- マイナンバー登録とログイン画面が追加されていない。
- メール通知登録が追加されていない。

## 実装後の報告

作業完了後、次の内容を簡潔に報告してください。

- 変更または作成したファイル
- 実装したルートと主要機能
- データの保存方式
- 実行方法
- 実行したテストまたは動作確認内容
- 未実装の機能があれば、その理由と範囲

上記仕様に沿って、必要なファイルを作成・編集し、アプリを起動して動作確認できる状態まで実装してください。既存システムへ組み込む場合は、既存の認証、ルーティング、データベース、CSS、コンポーネントを優先して再利用し、無関係な機能を変更しないでください。
# 防災アプリの「指示発信画面・地図機能」を再現するための実装指示

あなたはシニアWebアプリケーションエンジニアです。以下の要件を満たす、動作する防災アプリの一部を実装してください。

## 目的

既存システムから、次の機能だけを別のシステムへ移植します。

- 管理者向けの「指示・発信」画面
- 住民向けトップページの避難所マップ
- 指示発信画面内の地域マップ
- 避難所データの登録・一覧表示・検索
- 指示データの保存と、住民向けトップページへの表示

防災アプリ全体や気象警報機能など、上記に直接関係しない機能は追加しないでください。

## 技術条件

- バックエンドはPythonのFlaskを使用する。
- テンプレートエンジンはJinja2を使用する。
- フロントエンドはHTML、CSS、JavaScriptで実装する。
- 地図はLeaflet 1.9.4をCDNから読み込み、OpenStreetMapのタイルを使用する。
- 外部地図タイルには必ず次の帰属表示を付ける。
  `© OpenStreetMap contributors`
- 既存のプロジェクト構成や命名規則がある場合は、それに合わせる。
- データベースが既にある場合は既存DBを使い、ない場合はJSONファイルなどの永続化方式を使う。
- 画面は日本語で表示する。
- PCとスマートフォンの両方で崩れないレスポンシブUIにする。
- 認証情報、秘密鍵、パスワードは本番コードにハードコードせず、環境変数または既存の認証基盤を利用する。

## 対象地域と初期地図

初期表示地域は青森市とする。

- 表示名: 青森市
- 緯度: `40.8221`
- 経度: `140.7474`
- 初期ズーム: `12`

地図は次のように初期化する。

- トップページの地図要素ID: `shelterMap`
- 指示発信画面の地図要素ID: `boardMap`
- 地図コンテナの高さはPCではおよそ360px、指示発信画面ではおよそ320px、スマートフォンではおよそ280〜300pxとする。
- 地図コンテナは角丸にする。
- Leafletのズームコントロールを表示する。

## 画面構成

### 1. 共通ヘッダーとナビゲーション

全対象画面に共通ヘッダーを表示する。

- ヘッダーには「防災アプリ」と表示する。
- 青系の横長ヘッダーにする。
- 右側にハンバーガーメニューボタンを置く。
- ボタンを押すとナビゲーションを開閉する。
- `aria-label` と `aria-expanded` を設定し、キーボードやスクリーンリーダーでも扱えるようにする。
- ナビゲーションには以下のリンクを置く。
  - ホーム
  - 避難所検索
  - 避難所登録
  - 指示・発信ボード
  - ログインまたはログアウト
- 既存システムにルーティング機構がある場合は、その機構に合わせてリンクを生成する。

### 2. 住民向けトップページ

トップページに「避難所マップ」のセクションを表示する。

- 見出しは「避難所マップ」とする。
- `shelterMap` というIDの地図を表示する。
- 青森市を中心に地図を表示する。
- OpenStreetMapのタイルを表示する。
- 現在の移植対象の最低限の挙動では、地域を中心とした地図を表示する。避難所マーカーを実装する場合は、各避難所に緯度・経度が存在する場合だけ表示し、位置情報がない避難所を推測して配置しない。
- 地図の下または同じページ内に、指示データのうち対象が「住民」のものを表示する。
- 新しい指示を上に表示する。
- 指示には少なくとも本文、避難先（入力されている場合）、発信日時、ステータスを表示する。
- 保存された指示がない場合も画面が壊れないようにする。

### 3. 管理者向け「指示・発信」画面

URLは既存システムの規約に合わせる。Flask単体なら `/board` を使用する。

この画面はログイン済みの管理者だけが閲覧・送信できるようにする。未ログインの場合はログイン画面へリダイレクトし、ログイン後は元の画面へ戻す。

画面タイトルは「指示・発信」とする。フォームは次の項目を持つ。

#### 基本項目

- 対象のセレクトボックス（必須）
  - 未選択
  - 市民向け（保存値は「住民」）
  - 市職員向け（保存値は「防災課」）
  - 全員向け（保存値は「全員」）
- 災害の種類のセレクトボックス
  - 未選択
  - 津波
  - 河川氾濫
  - 道路寸断
  - 土砂崩れ
  - 避難勧告
  - 警告
- 地区のセレクトボックス
  - 未選択
  - 青森地区
  - 浪岡地区
  - 〇〇地区
- 発信内容のテキストエリア（必須）
  - プレースホルダー: 「指示・発信する内容を入力」

#### 指示発信画面内の地図

- 基本項目の近くに `boardMap` を表示する。
- 青森市を中心に、緯度 `40.8221`、経度 `140.7474`、ズーム `12` で初期化する。
- OpenStreetMapのタイルと帰属表示を設定する。
- 青森市の座標にマーカーを置く。
- マーカーのポップアップに「青森市」と表示し、初期状態で開く。
- 地図が表示されるまでフォームのレイアウトや入力欄がずれないよう、コンテナに固定または安定した高さを設定する。

#### 避難所情報の更新

「避難所情報を更新する」セクションを設ける。

- 登録済み避難所を選べるセレクトボックス
- 混雑状況のラジオボタン
  - 混雑
  - やや混雑
  - やや空いている
  - 空いている
- 施設情報のチェックボックス
  - ペット可
  - 障害者トイレ
  - ベビー室
- 備考欄のテキストエリア
- ファイルアップロード欄

この移植の最低限の挙動では、フォーム送信時に保存する指示データへ避難所名、混雑状況、備考を含める。施設情報と添付ファイルを保存する機能まで実装する場合は、保存先・ファイル形式・容量制限・アクセス権限を明確に設計する。保存処理を実装していない場合、画面にあるだけで送信後に保存されたように見せない。

#### メール配信欄

「メール配信をする」セクションを設ける。

- 配信タイトルの入力欄
- 本文のテキストエリア

実際のメール送信基盤が指定されていない場合は、タイトルと本文を指示データに保持するだけにし、メール送信済みと表示しない。

#### 送信操作

- 「ホームへ戻る」リンクを表示する。
- 「送信」ボタンを表示する。
- 対象または発信内容が空の場合は保存せず、画面上に「対象と本文を入力してください。」と表示する。
- 入力エラー時は、それまでに入力した値をフォームへ戻す。
- 正常送信時は現在時刻を日本時間で記録し、指示一覧の先頭へ追加する。
- 正常送信後は指示発信画面へリダイレクトする。
- 画面下部に「最近の発信」を表示し、最新3件を表示する。

## データ仕様

### 避難所

最低限、次の形式で扱えるようにする。

```json
{
  "id": 1,
  "name": "御所見小学校"
}
```

初期データには次の3施設を用意する。

- 御所見小学校
- 片瀬小学校
- 鵠洋小学校

避難所登録画面では、管理者が避難所名を入力して登録できるようにする。

- 空の避難所名は登録しない。
- 登録成功時は成功メッセージを表示する。
- IDは既存最大ID + 1とする。
- 登録後もデータが保持されるようにする。

避難所検索では、登録済み避難所の一覧を表示する。地区フィルターを持つ場合、地区が指定されたときだけ一致する避難所を返す。該当なしの場合は「該当する避難所が見つかりませんでした」と表示する。

### 指示

次のフィールドを保存できるようにする。

```json
{
  "id": 1,
  "target": "住民",
  "disaster_type": "津波",
  "district": "青森地区",
  "content": "避難してください",
  "shelter": "御所見小学校",
  "status": "発信中",
  "note": "",
  "email_subject": "",
  "email_body": "",
  "created_at": "2026年09月10日 12:00",
  "updated_at": "2026年09月10日 12:00"
}
```

- `id` は既存最大ID + 1とする。
- `status` が空の場合は「発信中」にする。
- `created_at` と `updated_at` は日本時間で記録する。
- 新しい指示を一覧の先頭へ追加する。
- トップページでは `target` が「住民」の指示だけを住民向け通知として表示する。
- 不正なJSON、存在しないデータファイル、空データでもアプリが起動できるようにする。
- ファイル保存を使う場合、書き込み失敗時はユーザーへエラーメッセージを表示し、メモリ上だけ保存された状態を残さない。

## ルートとAPI

Flaskで実装する場合の基準ルートは次のとおり。

- `GET /`: 住民向けトップページ
- `GET, POST /login`: 管理者ログイン
- `GET /logout`: ログアウト
- `GET, POST /board`: 管理者向け指示発信画面
- `GET, POST /shelter_register`: 管理者向け避難所登録
- `GET /shelter_search`: 避難所検索画面
- `GET /all_shelters`: 全避難所一覧
- `GET /search_results`: 地区などの条件で避難所を検索
- `GET /shelters?district=地区名`: 避難所をJSONで返すAPI

`shelters` APIの仕様:

- 該当する避難所がある場合はJSON配列をHTTP 200で返す。
- 該当しない場合は `{"error":"No shelters found"}` 相当のJSONをHTTP 404で返す。

## UIスタイル

- 青系のヘッダーと見出しを使う。
- ページ全体は白を基調とし、フォーム領域は薄い影と角丸で区切る。
- 入力欄は幅100%、薄いグレーの背景、1pxの境界線、8px程度の角丸にする。
- 指示発信フォームはPCでは3列を基本とし、700px以下では1列に切り替える。
- ボタンは青色で、送信ボタンであることが明確に分かるようにする。
- スマートフォンではフォーム、地図、ボタンが画面幅を超えないようにする。
- 既存システムにデザインシステムがある場合は、この見た目より既存の規約を優先する。

## セキュリティと品質

- Jinja2の自動エスケープを有効にする。
- POSTの入力値をサーバー側でも検証する。
- 管理者限定ページと登録・更新処理には認証を要求する。
- 外部リダイレクトの `next` パラメータは同一ホスト宛てだけ許可する。
- 本番環境では固定の秘密鍵やサンプルパスワードを使わない。
- ファイルアップロードを実装する場合は、拡張子、MIMEタイプ、サイズ、保存先、ファイル名を検証する。
- Leafletまたは地図タイルが読み込めない場合でも、フォームや他のテキスト情報は利用できるようにする。

## 受け入れ条件

実装後、次をすべて確認してください。

1. 未ログインで指示発信画面へアクセスするとログイン画面へ遷移する。
2. ログイン後に指示発信画面を開ける。
3. 対象と発信内容を入力して送信すると、指示が保存され、最近の発信の先頭に表示される。
4. 対象または発信内容が空の場合、保存されず、エラーメッセージと入力値が表示される。
5. 指示発信画面に青森市中心のLeaflet地図と「青森市」マーカーが表示される。
6. トップページに避難所マップが表示される。
7. 登録済み避難所が指示発信画面の避難所セレクトボックスに表示される。
8. 避難所を登録すると、再読み込み後も一覧に残る。
9. 住民向け指示だけがトップページの住民向け通知に表示される。
10. PC幅とスマートフォン幅で、フォーム・地図・ナビゲーションが横にはみ出さない。
11. データが空または存在しない状態でも、アプリが500エラーで停止しない。

## 実装時の出力

作業を終えたら、次を簡潔に報告してください。

- 変更または作成したファイル
- 実装したルートと主要機能
- データの保存方式
- 実行方法
- 実行したテストまたは動作確認内容
- 未実装の機能があれば、その理由と範囲

上記仕様に沿って、必要なファイルを作成・編集し、依存関係を設定し、アプリを起動できる状態まで実装してください。既存システムへ組み込む場合は、既存の認証、ルーティング、データベース、CSS、コンポーネントを優先して再利用し、無関係な機能を変更しないでください。
